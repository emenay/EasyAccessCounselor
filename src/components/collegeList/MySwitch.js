import React, {useState} from 'react'
import Switch from '@mui/material/Switch';

export default function MySwitch({toggle, cur}) {
    // const label = { inputProps: { 'aria-label': 'Switch demo' } };
    // const [state, setstate] = useState(cur)
    return (
        <td>
            {/* <Switch {...label} checked={cur} onChange={(e)=>{toggle(); setstate(!state)}}/> */}
        </td>
    )
}
